#posts
	- @post.each do |post|
		.post
			.post_image
				= link_to image_tag post.avatar.url(:thumb).to_s
			.post_content
				.title
					%h2= link_to post.name, post
				.data.clearfix
					%p.username
						Posted by
						= post.user&.username || 'N/A'